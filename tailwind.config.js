// tailwind.config.js
module.exports = {
    darkMode: 'media', // auto-detect system preference for light/dark mode
    content: ['./src/**/*.{js,ts,jsx,tsx}'], // ensure it scans your app files
    theme: {
      extend: {},
    },
    plugins: [],
  }